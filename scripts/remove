#!/bin/bash

domain=$(sudo yunohost app setting reverse_proxy domain)
UUID=$(sudo yunohost app setting reverse_proxy uuid)

sudo rm -f "/etc/nginx/conf.d/$domain.d/reverse_proxy_$UUID.conf"

sudo service nginx reload